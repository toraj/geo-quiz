// Import actions from '../actions'

const INITIAL_STATE = {}

export default function(state = INITIAL_STATE, action) {
  const { type } = action

  switch (type) {
    
    default:
      return state
  }
}
